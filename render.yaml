services:
  - type: web
    name: hackIDE_project
    env: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: gunicorn hackIDE_project.wsgi
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
